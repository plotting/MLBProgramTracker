# MLB The Show 26 Program Tracker — Python dependencies
#
# The tracker runs on Python's standard library only.
# Install the one optional package below for reliable Chrome cookie decryption:
#
#   pip install -r requirements.txt

cryptography>=41.0   # AES-GCM decryption of modern Chrome cookies (v10 format)
                     # Falls back to Windows DPAPI if not installed, but
                     # modern Chrome versions require this for cookie access.
